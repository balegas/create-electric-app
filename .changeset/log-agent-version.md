---
"@electric-agent/studio": patch
---

Log the agent package version from the sandbox to the web UI after project setup. Emit error log events to the UI when session create/resume flows fail. Fix PR preview sprites auth to use dedicated SPRITES_API_TOKEN.
