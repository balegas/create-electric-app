---
"@electric-agent/studio": patch
---

Fix Claude Code iterate in Sprites/Docker sandboxes by respawning with --resume instead of writing to dead stdin. Add Vite allowedHosts guardrail to generated CLAUDE.md.
