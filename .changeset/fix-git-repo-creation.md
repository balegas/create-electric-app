---
"@electric-agent/studio": minor
---

Inject git instructions into CLAUDE.md so the agent creates GitHub repos and pushes code when configured. Previously, repo config was collected from the user but never acted upon. Also removes the deprecated `generateElectricAgentClaudeMd` function.
