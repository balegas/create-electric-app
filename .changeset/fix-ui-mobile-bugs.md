---
"@electric-agent/studio": patch
---

Fix UI bugs: gate color now turns blue immediately after responding (instead of losing color until agent stops), agents announce in room channel when picking up a DONE task, and room prompt bar stacks correctly on mobile with 3 elements.
